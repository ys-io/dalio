import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    marginBottom: 8,
  },
  subtitle: {
    marginBottom: 40,
    lineHeight: 24,
  },
  doneTitle: {
    marginBottom: 16,
  },
  doneSubtitle: {
    marginBottom: 40,
    lineHeight: 24,
  },
  fieldMargin: {
    marginBottom: 32,
  },
  buttonMargin: {
    marginBottom: 12,
  },
});

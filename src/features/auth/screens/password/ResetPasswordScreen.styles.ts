import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    marginBottom: 8,
  },
  subtitle: {
    marginBottom: 40,
    lineHeight: 24,
  },
  fieldMarginSmall: {
    marginBottom: 4,
  },
  fieldMargin: {
    marginBottom: 32,
  },
});

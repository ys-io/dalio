import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 4,
    marginBottom: 16,
  },
  rules: {
    gap: 4,
  },
  ruleRow: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
  },
  iconContainer: {
    width: 14,
    alignItems: "center",
  },
  ruleText: {
    fontSize: 12,
  },
});
